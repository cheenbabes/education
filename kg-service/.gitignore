db.lock
db.wal
.kg_build_hash
extracted/
handoffs/
data/clusters/embeddings_*.npy
# Standards-search embeddings — lazily rebuilt per (state, grade) on first
# query, cached here. ~14MB+ so we keep them out of git; KG re-embeds on cache miss.
data/embeddings/
# NOTE: db and data/standards/*.json are committed — pre-built data for deploy
__pycache__/
*.pyc
.env
.venv/
.venv*/
